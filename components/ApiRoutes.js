const BASE_URL = "http://localhost:8000/";
const REGISTER = `${BASE_URL}register`;
const LOGIN = `${BASE_URL}login`;
const STUDENTS_BY_GRADES = `${BASE_URL}students-vs-grade`;
const STUDENTS_BY_DOJ = `${BASE_URL}students-vs-doj`;
const STUDENTS_BY_AGE = `${BASE_URL}students-vs-age`;
const STUDENTS_CREATE = `${BASE_URL}students`;
const STUDENTS_BASE = `${BASE_URL}students`;

export {
  REGISTER,
  LOGIN,
  STUDENTS_BY_GRADES,
  STUDENTS_BY_DOJ,
  STUDENTS_CREATE,
  STUDENTS_BASE,
  STUDENTS_BY_AGE,
};
